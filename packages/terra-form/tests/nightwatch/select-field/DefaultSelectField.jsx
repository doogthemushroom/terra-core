import React from 'react';

import SelectField from '../../../lib/SelectField';

const selectfield = () => (<SelectField id="default-select-field" choices={[{ value: 'm', display: 'moo' }]} />);

export default selectfield;
